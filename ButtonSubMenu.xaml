<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	x:Class="Firefly.Wpf.MenuDemo.ButtonSubMenu"
	x:Name="UserControl"
	d:DesignWidth="187" d:DesignHeight="42">
	<UserControl.Resources>
		
	</UserControl.Resources>

	<Grid x:Name="LayoutRoot">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="0.2*"/>
		</Grid.ColumnDefinitions>
		<Button x:Name="SubMenuButton"  Style="{DynamicResource SubMenu}" Grid.Column="1" />
		<Button x:Name="ClickButton" Content="noam" Style="{DynamicResource ButtonMEnu}"  />
		</Grid>
</UserControl>